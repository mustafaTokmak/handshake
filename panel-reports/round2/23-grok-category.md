**(a) Holds.** Unwind is a second-order product. The first minute is spent *inventing* a disaster so the next two can autopsy it. Infrastructure pitches work on buyers who already have the scar; this room does not. The stage line asserts the scar. Assertion is a keynote, not a demo.

**(b) Holds, and it is worse than “the world is fake.”** Fake is survivable if the object is visceral (money, an outage, a theft). Unwind’s object is a table of classifications over JSON. The only real side effect is `git revert` on a toy repo — and it is first on the cut list. That is an admission.

**(c) Holds.** Undo-of-undo is a ledger property, not an agent property. Given an irreversible email, the recursive claim is operationally garbage (unsend the retraction? resend the termination?). It is a 25-second CS joke that eats the Logfire slot every other Pydantic team will also show.

**(d) The panel did not converge.** Ten #1s: Canary, ChangeProof, SIEGE, Unwind, Rogue Intern, GameSmith, Collected, RetroAgent, 3AM, Airlock. Unwind is one contrarian pick plus a synthesizer averaging a shared prior: *fake company, typed tools, frame inversion, “missing production primitive,” all four logos.* That prior selects elegant blog posts. It does not select the image a tired judge can still describe at 20:50.

---

**1. Strongest case this pick is wrong**

You optimized for a design-review artifact, not a stage image.

Unwind’s value is conditional on four beliefs the first 20 seconds never earn: agents will get write access; they will be wrong; cross-system rollback is the bottleneck; a sim is evidence. The wow is a *classification* (9 reversible / 1 conflict / 1 irreversible), not an *event*. At award time judges remember one picture. Yours is a spreadsheet.

Sponsor-fit is also misread. Conduct’s objection is “don’t let it write,” not “undo after it did.” Undo is what you pitch after you lost the deal. Modal is garnish: the only Modal-native beat (fork/dry-run) is cut #2, so the authenticity claim is already abandoned on paper. “Nobody else will build rollback” is not a reason it wins; unique and illegible fail together.

**2. Single most likely failure**

**20:01, the product, not the setup.** The 25s fixture rule covers the actor mess. It does not cover Gemini Pro walking five serial stages (read → classify → synthesise → plan → dry-run). The script gives the table 40s (1:05–1:45). You get a spinner. You narrate. The CONFLICT/IRREVERSIBLE rows are on screen for eight seconds. There is no applause beat.

Build-day preview of the same bug: **16:30, conflict detection.** Whole-object snapshots make every field look conflicted, or none. You special-case `notes`. At 20:00 the judge edits `status`. No yellow row. The anti-canned move is the thing that exposes the hardcode.

Also a logic bomb in the script itself: you cannot have a preserved human edit, an unsent-unable email, *and* “residual delta 0 of 12” unless the denominator is cooked. A technical judge will notice. If they don’t, you spent the afternoon arguing about the metric.

**3. Legible at demo #12?**

No. The wow does not land without a 30s explanation you do not have.

A tired judge will not hold Acme Ltd vs Acme Corp vs 14 mutations vs a live edit vs saga semantics. Fourteen streaming rows are noise. “We will not clobber your edit” is HTTP 409. “Email cannot unsend, retraction drafted” is Outlook. Asking a judge to click a fake CRM they have never seen is a 15s tax that often just… doesn’t happen.

SIEGE is worse intellectually and better optically: grey → red is processed without a counterfactual. Unwind needs one, and then needs you to *care* that nine JSON fields reverted.

**4. SIEGE was not wrongly rejected — as written.**

The 5–15% lottery is real. You cannot honestly say “I have never seen that one before” *and* guarantee a breach. Repairing it (planted confused-deputy, deterministic exploit, patch, retest) makes the visual reliable and turns the wow into a lie. Then it is CTF with Modal, in a category that *is* crowded, and DeepMind will smell the plant in Q&A.

Reject the swarm. Do not promote it to Plan A. Do not keep Unwind just because SIEGE is also flawed.

**5. Highest-leverage change**

Invert the order: **preview-and-gate before any write, don’t autopsy after.** Same typed world, same Modal fork, same irreversible/conflict rows — as a simulation of a *proposed* plan. The room sees blast radius and a red REFUSED, then stamps a rewrite. No counterfactual. No 55s of staged arson. The irreversible-email beat becomes “we would have sent this; we didn’t.”

If you refuse to change the idea: throw out the three-pane IDE. One full-screen customer card, destroyed in eight seconds, partially resurrected, email left burned. The table is a subtitle. Cut “the undo is undoable.” Never say residual 0.

**6. Verdict**

**BUILD SOMETHING ELSE — Airlock: blast-radius refusal + rewritten plan + human stamp, before a write lands.**

That is Unwind’s actual insight, first-order. It is also what Grok already designed; the synthesizer passed it over because it looked less like “inverse planning,” which is the training prior talking.
